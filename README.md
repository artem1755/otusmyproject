# 🧪 UI автотесты проекта Otus

[![Java](https://img.shields.io/badge/Java-21-blue)](https://www.oracle.com/java/technologies/javase/jdk21-archive-downloads.html)
[![Maven](https://img.shields.io/badge/Maven-3.9.0-blue)](https://maven.apache.org/)

---

## 📖 Описание

Проект содержит **UI автотесты**, написанные на **Java 21** с использованием **Selenium** и **JUnit 5**.  
Для внедрения зависимостей используется **Google Guice**, а сборка и запуск тестов выполняются через **Maven**.

Проект построен по принципу **Page Object Model (POM)**, что делает код тестов структурированным, читаемым и легко
расширяемым.  
В будущем планируется добавить **API тестирование**.

---

## ⚙️ Используемые технологии

- ☕ **Java 21**
- 🧱 **Maven** — управление зависимостями и запуск тестов
- 🧪 **JUnit 5** — тестовый фреймворк
- 🌐 **Selenium WebDriver** — автоматизация UI
- 🧩 **Google Guice** — внедрение зависимостей (Dependency Injection)
- 🧰 **Checkstyle** и **SpotBugs** — статический анализ кода

---

## 🚀 Как запустить тесты

### 🧰 Требования

Перед запуском убедитесь, что установлены:

- **Java 21**
- **Maven**
- **IntelliJ IDEA** (рекомендуется для удобства запуска)

Проверьте версии:

```
java -version
mvn -version
```

## ▶️ Запуск из командной строки

### Клонируйте репозиторий:

```
https://github.com/artem1755/otusmyproject
cd otusmyproject
```

### Выполните команду для сборки и запуска тестов:

```
mvn clean test
```

#### Либо для сборки с параметрами:
```
mvn clean test -Dbase.url=https://otus.ru -Dbrowser.name=chrome
```

## Настройки окружения

Все настройки проекта находятся в файле config.properties.
Он позволяет задавать параметры браузера, базового URL и таймаутов.

```
browser=chrome
base.url=https://example.com
timeout=10
```